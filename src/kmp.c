#include "kmp.h"

#include <stdio.h>
#include <stdlib.h>

void KMP(note * original, int M, note * suspect, int T) {
	fprintf(stderr, "ERROR: %s not implemented yet.\n", __func__);
	exit(EXIT_FAILURE);
}
